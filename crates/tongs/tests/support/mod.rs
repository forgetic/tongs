//! Shared support for the **subject recording** harness (`subject_record.rs`):
//! credential resolution against the real auth file and the scenario driver
//! that records tongs' own wire requests through jig's passthrough recorder.

pub mod auth;
pub mod subject;
